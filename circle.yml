machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t mahnve/flask-demo .
test:
  override:
    - nosetests
deployment:
  hub:
    branch: master
    commands:
      - docker push mahnve/flask-demo
